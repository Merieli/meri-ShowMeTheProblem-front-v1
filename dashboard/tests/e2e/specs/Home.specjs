describe('Home', () => {
  const APP_URL = process.env.APP_URL || 'http://localhost:8080'
  describe('🧪 Unidade:', () => {
    it('Dado o modal de login Quando clicar no botão de cta Então deve abrir o modal', () => {
      cy.visit(APP_URL)

      cy.get('#create-account-button').click()

      cy.get('#modal-create-account')
    })
  })
})
