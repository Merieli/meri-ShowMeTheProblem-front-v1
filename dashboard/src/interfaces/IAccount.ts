export interface IAccount {
    name: {
        value?: string
        errorMessage?: string
    }
    email: {
        value?: string
        errorMessage?: string
    }
    password: {
        value?: string
        errorMessage?: string
    }
}
