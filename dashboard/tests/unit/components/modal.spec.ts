/*
import PartModal from '@/components/Modal/index.vue'
import { shallowMount, VueWrapper } from '@vue/test-utils'

let wrapper: VueWrapper
beforeAll(() => {
    wrapper = shallowMount(PartModal, {})
})


describe('Component PartModal', () => {
    it('Deveria emitir o evento de close ao clicar no botão fechar', () => {
        const closeButton = wrapper.find('.modal__button')

        console.log(closeButton)

        closeButton.trigger('click')

        expect(wrapper.emitted().close.length).toBe(1)
    })
})
*/
